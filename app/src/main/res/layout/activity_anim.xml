<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#333"
    android:orientation="vertical">


    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="10dp"
        android:background="#fff"
        android:gravity="center"
        app:logo="@mipmap/ic_launcher"
        app:navigationContentDescription="返回"
        app:navigationIcon="@mipmap/ic_launcher_round"
        app:subtitle="子标题"
        app:subtitleTextAppearance="@style/mySubToolbar"
        app:subtitleTextColor="#666"
        app:title="主标题"
        app:titleTextAppearance="@style/myToolbar"
        app:titleTextColor="#000">

    </android.support.v7.widget.Toolbar>


    <android.support.v7.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="#ffffff"
        app:navigationIcon="@mipmap/ic_launcher_round">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:drawableRight="@mipmap/ic_launcher_round"
            android:hint="搜索"
            android:textColorHint="#999" />
    </android.support.v7.widget.Toolbar>


    <Button
        android:id="@+id/startAnim"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_margin="5dp"
        android:onClick="myOnClick"
        android:text="启动动画"
        android:textAllCaps="false" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="5dp"
        android:background="#999">

        <com.wesai.kotlin.modules.AnimView
            android:id="@+id/animView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <View
            android:id="@+id/pathView"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="#8fff8899"></View>


    </FrameLayout>

</LinearLayout>
